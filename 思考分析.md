我们在面对现实生活中办公的问题时，往往在一开始很容易大脑一片空白，无从下手。

而事实上，几乎所有的这类问题，都可以按照一个统一的框架去思考，然后去找到它的解题思路。

这个框架就是：「输入-输出」框架。
遇到一个问题后，抛开所有的代码细节和实现，我们要首先分析这个问题输入的数据是什么？

拿最简单的文件分类问题举例，这个问题输入的数据就是：指定文件夹下所有的文件。

然后，是确定输出的数据是什么？文件分类问题的输出数据是：指定文件夹内已经分类好的文件夹。
确定了输入和输出的数据后，下一步则是要去思考：从输入的数据要得到输出的数据需要哪些步骤，同样在这时依然可以不考虑代码的实现。

例如未分类的文件要得到分类好的文件夹需要的步骤就是：

1. 判断文件类型
2. 创建对应的分类文件夹
3. 移动文件到文件夹内

获得了这些步骤后，就可以去查找如何用代码实现了。

下面我们来思考一个相对复杂的问题来作为练习。

复杂问题在我们按照“「输入-输出」框架”一步一步的分析下就得出解题步骤。

我们再来复习一下整个框架的思考过程：

1. 这个问题的输入数据是什么？
2. 这个问题的输出数据是什么？
3. 要得到输出数据，输入的数据需要经过哪些步骤？

当在进行第3步时，有可能输入的数据无论怎么想步骤都无法得出输出的数据。这时候可以尝试思考是不是缺少了一些输入的数据。

# 获取新知识
用搜索引擎查询Python相关知识点的方法是使用Python + 关键词来进行搜索。

例如，我们想知道Python中列表内的元素如何保证不重复，我们就可以用关键词“Python 列表 不重复”这个关键词来进行搜索，搜索到的结果如图。

# 捉虫
当程序出现错误的时候，首先能够帮助我们快速定位错误位置的是“报错信息”。

报错信息中主要的信息有三个：第一是报错的文件路径，它能够帮助我们在文件模块较多时快速定位到出错的文件。

第二是报错的行数，这个信息比较关键，它能够迅速帮我们定位到具体是哪一行报的错。

第三是错误类型，它说明了对应行是为什么报错

当程序代码逻辑比较复杂时，如果出现运行结果与预期不符，会很难定位问题。

一种简单但有效的定位问题方式是使用print()函数一步一步把关键结果输出出来。

多运行，多输出反馈结果。

当发现输出后发现某一步的结果有问题，这样就可以缩小查找问题的范围。
