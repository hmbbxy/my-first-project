#  PPT 模版里的三个概念：
**母版、版式和占位符**
+ 版式
  
打开一个 PPT，添加一张幻灯片页，就会出现带有虚线框的页面，这就是“版式”。

PPT 中有很多自带的版式，是系统为使用者排版好的页面，只需要往里面填内容就好，十分的方便。

+ 母版

如果你想要自己设计版式，就需要专门修改这些版式的功能——母版。

在母版中有很多版式，排在第一位的版式，也就是新建一页的页面。

+ 母版与版式的区别

放在“母版”中的任何元素，都会直接出现在每一页PPT中；

放在"版式"中的元素，则需要手动调出来才会显示在某页PPT中。

+ 占位符

占位符，顾名思义，就是一种用来占位置的“符号”，也就是版式中的虚线框。占位符的编辑需要通过母版进行操作。

有了占位符，你可以输入文字、插入图片，并且不需要调整格式。

<img width="780" height="600" alt="P12-" src="https://github.com/user-attachments/assets/55928e7d-7628-4e0c-af36-987513a729c2" />

# 读取 .pptx 文件并在幻灯片页中添加内容。
+ 首先，我们要使用 python-pptx 模块，调用 Presentation() 函数，读取 .pptx 文件
```python
# 使用from...import从pptx模块中导入Presentation
from pptx import Presentation

# .pptx文件路径赋值给变量path
path = "/Users/工牌.pptx"

# 读取PPT赋值给pptxFile
pptxFile = Presentation(path)
```

+ 读取“工牌.pptx”文件后，接下来就要往文件中新增一张幻灯片页。

由于我们要添加工牌模版，它是母版中的第一个版式，在这里，我们需要先获取版式，再添加页面

## 获取版式对象
+ 在 Presentation 对象中访问 .slide_layouts 函数可以获得所有版式对象。
+ 对版式序列进行索引，可以获取指定版式对象。
`layout = pptxFile.slide_layouts[0]`
<img width="780" height="600" alt="P20-1" src="https://github.com/user-attachments/assets/cbc292d1-9792-418e-a2d0-91f0c93d4c34" />

## 添加幻灯片页
+ 在 Presentation 对象中访问 .slides 属性，返回幻灯片页序列
+ 在幻灯片页对象中，调用 add_slide() 函数，传入幻灯片版式对象，即可在 .pptx 文件中新增指定版式的幻灯片页
  + 在 add_slide() 函数中传入指定版式对象，即可添加该版式页面
  
```python
layoutOne = pptxFile.slide_layouts[0]

slideAll = pptxFile.slides

newSlide = slideAll.add_slide(layoutOne)
```

***
## 要在模版页中添加文本和图片，其实也就是在占位符中添加内容。

仔细观察，在模版中有 4 个占位符，我们需要获取占位符及其属性，才能精确的写入对应的信息。

为了把信息写入指定占位符内，python-pptx 模块给每页中的占位符设置了编号和对应的类型。

+ 我们可以按照以下层级获取到占位符的编号和类型：
  
占位符对象 -> 占位符属性对象 -> 占位符编号和类型

## 获取占位符与属性
+ 访问幻灯片页对象中 .placeholders 属性，可以获得幻灯片页中占位符序列。

**占位符序列**中包含所有**占位符对象**，可以使用索引定位到单个占位符对象；

访问单个占位符对象中的 .placeholder_format 属性，可以获得此占位符的属性对象
```python
for part in slide.placeholders:
    phf = part.placeholder_format
```
## 占位符编号与类型
访问占位符的属性对象中的 .idx 属性，可以获得占位符的编号。

知道了占位符的编号，才可以指定添加内容。
```python
for part in slide.placeholders:    
    phf = part.placeholder_format
    print(phf.idx)
    print(phf.type)
```

### 占位符的类型
访问占位符的属性对象中的 .type 属性，可以获得占位符的类型。

在 PPT 中占位符的类型有 18 种，常见的有：

TITLE——标题占位符

BODY——正文占位符

PICTURE——图片占位符
```python
# 使用from...import从pptx模块中导入Presentation
from pptx import Presentation

# .pptx文件路径赋值给变量path
path = "/Users/工牌.pptx"

# 读取PPT赋值给pptxFile
pptxFile = Presentation(path)

# 获取母版中第一个版式，赋值给变量layoutOne
layoutOne = pptxFile.slide_layouts[0]

# 访问pptxFile中.slides属性，赋值给变量slideAll
slideAll = pptxFile.slides
# 调用add_slide()函数传入参数layoutOne
# 新建幻灯片页，并赋值给变量newSlide
newSlide = slideAll.add_slide(layoutOne)

# for循环遍历newSlide中的.placeholders属性
# 赋值给变量single_plh
for single_plh in newSlide.placeholders:
    # 访问占位符对象中的.placeholder_format属性
    # 赋值给变量phf
    phf = single_plh.placeholder_format
    # 访问phf中的.idx和.type属性
    # 格式化字符串以"-"连接并输出
    print(f"{phf.idx}-{phf.type}")

# 将文件至原路径
pptxFile.save(path)
```

```
1-PICTURE (18)
2-BODY (2)
10-BODY (2)
11-BODY (2)
```

从输出的结果来看，在模版页面中有一个图片占位符，编号为 1；有三个正文占位符，编号为 2、10、11。

获取到了占位符的编号，接下来就可写入对应的内容啦～

## 占位符添加文本
+ 访问幻灯片页对象中的 .placeholders 属性，获取页面中占位符序列。
+ 在占位符序列中索引编号定位到单个占位符对象。
+ 写入文本
  
访问指定正文占位符对象中的 .text 属性，可以写入文本内容
+ 注意，如果正文占位符中已经存在文本，.text 属性可以用新的文本替换已经存在所有文本。
+ 写入的文本内容需要是字符串的格式，否则就会报错。


```python
all_plh = newSlide.placeholders

all_plh[1].text = "yequ"
```

## 占位符添加图片
+ 在占位符序列中索引编号定位到单个占位符对象。
+ 在图片占位符对象中，调用 insert_picture() 函数，传入图片路径，即可在图片占位符中插入指定的图片
```python
all_plh = newSlide.placeholders

all_plh[2].insert_picture("/Users/yequ.png")
```

```python
# 使用from...import从pptx模块中导入Presentation
from pptx import Presentation

# .pptx文件路径赋值给变量path
path = "/Users/工牌.pptx"

# 读取PPT赋值给pptxFile
pptxFile = Presentation(path)

# 获取母版中第一个版式，赋值给变量layoutOne
layoutOne = pptxFile.slide_layouts[0]

# 访问pptxFile中.slides属性，赋值给变量slideAll
slideAll = pptxFile.slides
# 调用add_slide()函数传入参数layoutOne
# 新建幻灯片页，并赋值给变量newSlide
newSlide = slideAll.add_slide(layoutOne)

# 访问newSlide中的.placeholders，赋值给变量all_plh
all_plh = newSlide.placeholders
# 将"白涵"、"运营"、"35"写入对应的正文占位符
all_plh[2].text = "白涵"
all_plh[10].text = "运营"
all_plh[11].text = "35"
# 在图片占位符中写入图片
# 调用insert_picture()函数，传入图片路径
all_plh[1].insert_picture("/Users/wen/白涵.png")

# 将文件至原路径
pptxFile.save(path)
```

***
由于 PPT 模版中，需要写入的内容有：姓名、岗位、工号与头像二维码，而前三个信息都在 Excel 文件中。

在这里，我们需要导入相关模块，读取指定工作表，把姓名、岗位和工号提取出来。
```python
# 导入xlrd模块
import xlrd
# 将Excel文件夹路径/Users/wen/资料.xlsx，赋值给变量path
path = "/Users/wen/资料.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个工作表赋值给变量table
table = data.sheets()[0]
```

在读取了 Excel 文件中指定的工作表后，接下来我们要
##提取姓名、岗位和工号。

提取的方法有很多，在这里，我们可以按行读取工作表数据。

+ 通过调用工作表的 row_values() 函数，传入行数的索引值，就会返回本行全部数据组成的列表。
+ 再对列表进行索引，就可以得到指定的数据

+ 可以使用 for 循环遍历 range() 函数，生成 1 到 200 的整数，再把整数传入 row_values() 函数中，即可得到指定行的数据啦～(由于我们要得到工作表中，除第一行以外指定数据，就需要遍历第2行到第201行。)

```
# 导入xlrd模块
import xlrd
# 将Excel文件夹路径/Users/wen/资料.xlsx，赋值给变量path
path = "/Users/wen/资料.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个工作表赋值给变量table
table = data.sheets()[0]

# for循环遍历range()生成1到200的整数
# 赋值给变量row
for row in range(1,201):
    # 获取第row行数据列表，
    # 索引第1、2、5个元素
    # 分别赋值给job_num、name、post
    job_num = table.row_values(row)[0]
    name = table.row_values(row)[1]
    post = table.row_values(row)[4]
    # 格式化字符串输出xx-xx-xx
    # 以"-"连接job_num、name、post
    print(f"{job_num}-{name}-{post}")
```
***
## 查找头像二维码对应的信息，可以分为以下两个子步骤：
1. 获取文件夹中的文件名；
2. 分割文件名获取文件名称；

```python
# 导入xlrd模块
import xlrd
# 将Excel文件夹路径/Users/wen/资料.xlsx，赋值给变量path
path = "/Users/wen/资料.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个工作表赋值给变量table
table = data.sheets()[0]

# 导入os模块
import os
# 将文件夹路径"/Users/wen/二维码",赋值给pathFile
pathFile = "/Users/wen/二维码"
# 使用os.listdir()函数获取该路径下所有文件名，并赋值给变量allItems
allItems = os.listdir(pathFile)

# for循环遍历range()生成1到200的整数
# 赋值给变量row
for row in range(1,201):
    # 获取第row行数据列表，
    # 索引第1、2、5个元素
    # 分别赋值给job_num、name、job
    job_num = table.row_values(row)[0]
    name = table.row_values(row)[1]
    post = table.row_values(row)[4]
    
    # for循环遍历allItems，赋值给变量item
    for item in allItems:
        # 调用split()分割item，获取第一个元素赋值给变量file_name
        file_name = item.split(".")[0]
```

## 把员工信息批量写入PPT中
```python
# 导入xlrd模块
import xlrd
# 将Excel文件夹路径/Users/wen/资料.xlsx，赋值给变量path
path = "/Users/wen/资料.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个工作表赋值给变量table
table = data.sheets()[0]

# 导入os模块
import os
# 将文件夹路径"/Users/wen/二维码",赋值给pathFile
pathFile = "/Users/wen/二维码"
# 使用os.listdir()函数获取该路径下所有文件名，并赋值给变量allItems
allItems = os.listdir(pathFile)

# 使用from...import从pptx模块中导入Presentation
from pptx import Presentation
# 将.pptx文件路径赋值给变量pptxPath
pptxPath = "/Users/wen/工牌.pptx"
# 读取pptxPath并赋值给变量pptxFile
pptxFile = Presentation(pptxPath)
# 获取母版中第一个版式，赋值给变量layoutOne
layoutOne = pptxFile.slide_layouts[0]
# 访问pptxFile中.slides属性，赋值给变量slideAll
slideAll = pptxFile.slides

# for循环遍历range()生成1到200的整数
# 赋值给变量row
for row in range(1,201):
    # 获取第row行数据列表，
    # 索引第1、2、5个元素
    # 分别赋值给job_num、name、job
    job_num = table.row_values(row)[0]
    name = table.row_values(row)[1]
    post = table.row_values(row)[4]
    
    # for循环遍历allItems，赋值给变量item
    for item in allItems:
        # 调用split()分割item，获取第一个元素赋值给变量file_name
        file_name = item.split('.')[0]
        # TODO 如果文件名称与姓名相等
        if file_name==name:
            # TODO 在slideAll中添加母版中第一个版式，赋值给slide
            slide=slideAll.add_slide(layoutOne)
```

## 接下来，我们要把信息写入工牌版式中
需要获取版式中的占位符编号

在明确了每个占位符的编号后，接下来就要在占位符中添加文本和添加图片。

```python
# 导入xlrd模块
import xlrd
# 将Excel文件夹路径/Users/wen/资料.xlsx，赋值给变量path
path = "/Users/wen/资料.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个工作表赋值给变量table
table = data.sheets()[0]

# 导入os模块
import os
# 将文件夹路径"/Users/wen/二维码",赋值给pathFile
pathFile = "/Users/wen/二维码"
# 使用os.listdir()函数获取该路径下所有文件名，并赋值给变量allItems
allItems = os.listdir(pathFile)

# 使用from...import从pptx模块中导入Presentation
from pptx import Presentation
# 将.pptx文件路径赋值给变量pptxPath
pptxPath = "/Users/wen/工牌.pptx"
# 读取pptxPath并赋值给变量pptxFile
pptxFile = Presentation(pptxPath)
# 获取母版中第一个版式，赋值给变量layoutOne
layoutOne = pptxFile.slide_layouts[0]
# 访问pptxFile中.slides属性，赋值给变量slideAll
slideAll = pptxFile.slides

# for循环遍历range()生成1到200的整数
# 赋值给变量row
for row in range(1,201):
    # 获取第row行数据列表，
    # 索引第1、2、5个元素
    # 分别赋值给job_num、name、post
    job_num = table.row_values(row)[0]
    name = table.row_values(row)[1]
    post = table.row_values(row)[4]
    
    # for循环遍历allItems，赋值给变量item
    for item in allItems:
        # 调用split()分割item，获取第一个元素赋值给变量file_name
        file_name = item.split('.')[0]
        # 如果文件名称与姓名相等
        if file_name == name:
            # 在slideAll中添加母版中第一个版式，赋值给slide
            slide = slideAll.add_slide(layoutOne)
            # 占位符编号2的文本设置为name
            slide.placeholders[2].text = name
            # 占位符编号10的文本设置为post
            slide.placeholders[10].text = post
            # 占位符编号11的文本设置为字符串f"{job_num}号"
            slide.placeholders[11].text = f"{job_num}号"
            # 占位符编号1的插入图片路径
            # 字符串"+"拼接路径：pathFile+"/"+item
            slide.placeholders[1].insert_picture(pathFile+"/"+item)
            
# 将pptxFile保存至原路径
pptxFile.save(pptxPath)
```
