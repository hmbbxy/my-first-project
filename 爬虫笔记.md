# 爬虫笔记**markdown版**（ ~~努力学习中~~ ）
+ 我们学习过浏览器网页内容，是*由 HTML 代码组成*。看网页对应的 HTML 代码：
**按键盘的 F12** 或者**鼠标右键**选择检查，就可以看到浏览器开发者工具。

+ 想要获取网页中的数据，首先要获取网页 HTML 代码，再把数据从中提取出来
我们要向网页的服务器发送请求，服务器返回的响应就是网页 HTML 代码

+ 在 HTML 中，**span 标签**的作用是用来**组合**文档中的**行内元素**
**class 属性**的作用是用来给**标签分类**。class="short"也就代表着，这里标签的内容是短评。

+ 在网页中属性需要使用**x(点).属性值 来定位**。
例如，我们在检索框中输入 .short 进行搜索，就能看到该页中 class="short" 属性出现的次数为20次。
那么，就定位到了提取评论的节点 class="short" 
***
+ *标签*有时候又叫做*节点*
  :是用来标记内容块的，主要有两种形式成对出现和单独出现。
***
+ 调用 **.string 属性** ，用来获取节点中的内容(文字）
+ 使用 for 循环遍历列表，获取每一个节点字符串，再来调用 .string 属性获取节点中的标签里的内容
+ 备注：.string 属性只能提取单个节点或节点统一的内容。
##提取单个节点内容：
例如
**<p><em>夜曲编程 这是一个段落</em></p>**
p 节点包含一个子节点 em ，由于 p 节点有且只有一个子节点，使用 .string 属性时，会输出 em 节点的 .string 内容。
## 提取节点包含多个子节点时：
**<p><em>夜曲编程</em> 这是一个编程 </p>**
当定位的节点中同时包含了多个子节点时，示例中：有两个节点，*一个节点是 em 标签内容，另一个节点是纯文字。*
使用 .string 属性 属性时，不清楚应该调用哪个节点的内容，会返回None值。
**.get_text() 总是提取所有文本，.strip() 去掉首尾空格，不会因为嵌套标签而出错**

**遇到节点中既包含其他节点，也有文字时，可以使用 .text 属性来提取内容**
***
## 搜索标签
+ 打开网页 [飞屋环游记](https://nocturne-spider.baicizhan.com/2020/08/07/1/)
1. 选择检查，使用快捷键 Ctrl+F。页面的右下侧就会出现检索栏。
2. 在检索栏中输入*标签*p，按回车键，就能看到该页面中的 p 标签数量。
3. 使用搜索栏右侧的上下箭头，就能定位到每个 p 标签的位置
***
实例：
从豆瓣上爬取数据，你将会完成以下内容：
1. 向网页发送请求，获取网页源代码;
2. 解析源代码，提取想要的数据；
3. 使用 jieba 模块将语句切分成词；
4. 运用字典的知识进行词频统计；
5. 将词语生成词云图。

## 在网页中**请求头和响应头**信息保存在变量 headers 中，查看请求头或响应头的方法为：
1. 使用 响应消息.request.headers 就能查看请求头中的信息。
2. 使用 响应消息.headers 就能查看响应头中的信息。

请求头中的**User-Agent**用来表示产生请求时**浏览器的类型**。
查看使用 响应消息.request.headers 输出的请求头中, User-Agent 的值为 python-requests ，说明此次请求是Python发出的并且能被服务器识别到。
~~那么怎么伪装成浏览器发出请求呢~~
(具体看代码操作）
***
# 中文分词
+ 指的是将一个汉字序列切分成一个一个单独的词
jieba.lcut() 将字符串切分为词。
# 列表合并
+ 通过"+"可以合并两个列表的所有元素，其中加号右边列表的元素会添加在左边列表元素之后
# 词频统计
+ *补充：给字典添加内容-添加新键值对number_dict["d"] = 4*
<img width="2173" height="1075" alt="屏幕截图_7-8-2026_211134_np baicizhan com" src="https://github.com/user-attachments/assets/47173c8b-857d-4b86-831e-6536dc850dc6" />
<img width="2086" height="1007" alt="屏幕截图_7-8-2026_211524_np baicizhan com" src="https://github.com/user-attachments/assets/7a2f6741-f069-438a-aec3-1c498abb24fc" />

***

# 生成词云图
**pyecharts 模块中的 WordCloud 可用于生成词云图。**
<img width="1342" height="587" alt="屏幕截图_7-8-2026_214454_np baicizhan com" src="https://github.com/user-attachments/assets/32462ad0-1b2f-475c-99ad-fc04de4d4d47" />
## 调用 add() 函数可以设置词云图的内容，例如：展示文字，字体大小等。
+ series_name
系列名称（series_name）指的是数据的统称，例如：降水量、蒸发量。
series_name 为必须参数，就是必须传入，词云图中展示的是文字内容，在这里 series_name 可以设置为空，即 series_name=""。
注意：为空时，双引号中间没有空格。

+ data_pair
data_pair 参数是指传入词云图中的数据，它的值的格式为：
[(word1, count1), (word2, count2)]
这是一个由元组组成的列表，列表中的每项都是元组。
word 是词云图中的文字；count 是文字的词频。

+ items()
在这里，我们可以使用 items() 函数，将字典 wordDict中的每对 key 和 value 组成一个元组，并把这些元组放在列表中返回。
例如：对wordDict使用items()，wordDict.items(), 就能获得列表 [('君问', 150), ('归期', 130), ('未有期', 120)] 。
items() 函数的返回结果，刚好符合 data_pair 参数的要求，即 data_pair=wordDict.items() 。

+ word_size_range
是用来设置词云图中字体大小范围，它的数据类型是列表。
word_size_range 的默认值为[12, 60]，12代表着词云图中频率最低的词语大小，60代表着词云图中频率最高的词语大小。
在这里，我们可以修改参数值设置为[20, 80]，即 word_size_range = [20,80] 。
<img width="1319" height="378" alt="屏幕截图_7-8-2026_215755_np baicizhan com" src="https://github.com/user-attachments/assets/e2fab7bb-2515-451a-a0b2-0517df9c145b" />
<img width="1254" height="451" alt="屏幕截图_7-8-2026_215421_np baicizhan com" src="https://github.com/user-attachments/assets/d51c7cf1-d4c9-4d06-8c28-5f4e687e0cf8" />

## render() 函数默认将词云图以 html 格式存储在代码所在文件夹内。
+ 在本地使用VsCode运行代码后，代码左侧文件目录就能看到生成的html文件，或者以文件名进行搜索。
render() 中要传入字符串，即以双引号包围。
+ 字符串中要设置 html 格式的文件，即 文件名称+.+html ，例如：rain.html。
在目录中找到这个 html 文件，点击它，就可以在浏览器中看到生成的词云图啦～

***

# 批量处理海报
+ 在Python语言中，open()函数表示要打开一个文件，对文件进行处理的操作都需要用到这个函数
+ open("文件路径", "打开方式")
  <img width="1659" height="1036" alt="屏幕截图_7-8-2026_222124_np baicizhan com" src="https://github.com/user-attachments/assets/f64c5aa5-ae6e-4267-97f5-dde89c989099" />
  <img width="1720" height="879" alt="屏幕截图_7-8-2026_222219_np baicizhan com" src="https://github.com/user-attachments/assets/1d0062d6-0475-459d-b9cd-f901e53b2d61" />

+ with as
<img width="1039" height="608" alt="屏幕截图_7-8-2026_222848_np baicizhan com" src="https://github.com/user-attachments/assets/a2355120-b0cd-464b-8a64-ba9fde02281b" />

+ 通过链接获取网页中的图片数据，需要用到**.content属性**，使用该属性获取到的数据类型为bytes类型，表示二进制数据。

***
+ 高清图片
  <img width="1815" height="462" alt="屏幕截图_8-8-2026_94512_np baicizhan com" src="https://github.com/user-attachments/assets/6d0e3adb-225a-4af3-855a-bc4192cd04dd" />
# 替换字符串
+ replace()函数中要传入两个参数，第一个参数是字符串被替换的内容，第二个参数是要替换成的新内容

***
+ 补充：for循环生成的参数属于整型，不能直接与字符串进行拼接，需要将整型转为字符串再处理
<img width="1631" height="464" alt="屏幕截图_8-8-2026_23227_np baicizhan com" src="https://github.com/user-attachments/assets/50987dc0-ebc0-4af8-8329-1d04f5328478" />
## 整型转字符串
+ str()函数可用于将整型转成字符串格式
+ 将num传入type()并输出，结果是整型

***
+ 可以通过弹幕 API，获取一个视频的弹幕数据(API 其实就是别人已经写好的可以实现特定功能的函数)
+ 接口链接
将获取的 cid 参数值放入以下 URL 链接中。
https://comment.bilibili.com/{cid}.xml
例如：
https://comment.bilibili.com/218710655.xml
在浏览器中打开上面的 URL 链接，进入该网页就可以看到视频中所有弹幕数据。
返回的结果不是一般的网站，而是 XML 文件(可扩展标记语言，是一种标记语言。标记指计算机所能理解的信息符号，有了标记，我们可以通过程序很方便的识别数据的类型和层次结构。)
+ XML与HTML的区别
XML 被设计用来传输和存储数据，其焦点是数据的内容。
HTML 被设计用来显示数据，其焦点是数据的外观。

***
+ strip() 函数默认移除空格和换行符。
***
# 反爬虫代码(显示爬虫得到的方框实际内容）
+ 字符串反编码转换
使用 encode() 函数，实现“编码”。
在 encode() 函数中默认 UTF-8 编码，由于它是针对英语设计的，中英文字符转码后会有差异。
以代码为例，字母和数字经过编码后格式不变；每个中文字符对应三个“\”分割的字符内容。
```python
alp = "yequ"
num = "666"
word = "夜"
words = "夜曲" 
print(alp.encode())
print(num.encode())
print(word.encode())
print(words.encode())
*output:
b'yequ'
b'666'
b'\xe5\xa4\x9c'
b'\xe5\xa4\x9c\xe6\x9b\xb2'*
```
+ 替换字符 replace（）函数
+ 将新的二进制数据转换成字符串，这个过程就叫做**“解码”**，我们可以使用 decode() 函数。
***
+ "a" 可以在 txt 文件中追加添加新内容，也就是说，新的内容将会被写入到已有内容之后。如果该文件不存在，就创建新文件。
  <img width="1526" height="416" alt="屏幕截图_9-8-2026_193952_np baicizhan com" src="https://github.com/user-attachments/assets/674f354e-cd80-4ce0-9d5e-d5533ac10932" />
***
+ readlines() 函数可以读取 txt 文件中的所有行并返回一个列表。
```python
#使用with...as语句配合open()函数以r方式，打开文件，赋值给f
with open("/Users/tongtong/职位数据.txt", "r") as f:
#使用readlines()读取f中的所有行，赋值给dataList
  dataList=f.readlines()
#使用print输出dataList
print(dataList)
```
***
+ 想要遍历字典中的键和值，我们首先可以使用 items() 函数，将字典键对转换成元组数组。并把这些元组放在列表中返回
+ <img width="2213" height="1033" alt="屏幕截图_9-8-2026_203413_np baicizhan com" src="https://github.com/user-attachments/assets/c77c58ae-4445-49ee-80aa-68f20d41ad28" />
***
+ <img width="786" height="398" alt="屏幕截图_10-8-2026_143431_np baicizhan com" src="https://github.com/user-attachments/assets/643de0bc-a8a1-4043-8d7c-927ed2ada226" />
***
+ JSON的全称为JavaScript Object Notation，是一种用于存储和传输数据的格式
+ JSON是一种轻量级的文本数据交换格式，与xml类似，但比XML 更小、更快、更易理解和解析。
简单来说，JSON就像字符串、列表、元组一样，用来存储和传输数据
# JSON语法规则
1. 数据以 {键:值} 的形式呈现。
JSON数据与字典类似，是由键值组成的，每一对键值都是以“:”进行连接，键必须是字符串并由双引号包围，引号中填写具体内容
2. 数据与数据之间以逗号分隔。

<img width="420" height="753" alt="屏幕截图_10-8-2026_143714_np baicizhan com" src="https://github.com/user-attachments/assets/6151bfce-1d7d-4260-b5bb-610497615688" />
4. 方括号[]表示数组(数组的值存放格式与列表相似)

<img width="663" height="742" alt="屏幕截图_10-8-2026_143818_np baicizhan com" src="https://github.com/user-attachments/assets/b266b86c-6365-42cc-a3ef-874d8b60ee7b" />

5. 大括号{}用于保存对象。
***
+ `lstrip()`和`rstrip()`
`lstrip()` 可用于移除字符串左边的空格或字符串。
使用 `lstrip()` 函数后，需要赋值给新的变量
+ `rstrip()` 函数的括号中用于存放需要移除的最右边的字符，字符使用双引号包围起来。
使用 `rstrip()` 函数时，该函数从最右侧依次判断每个字符，遇到非指定字符内容就停止。
+ `json.loads()` 函数能够对数据进行解码，将JSON数据与Python的数据类型进行转化，目的是能在Python中处理JSON数据 。
使用 `json.loads()` 函数时，将JSON数据传入括号中就能将字符串类型数据转换成字典类型的数据。
解码成的数据类型需要看JSON对应的格式，格式为字典就会转成字典，为列表就会转成列表。

***
+ 某些网站的服务器会给用户浏览器发送一小块数据，用于辨别用户身份，这个就是 Cookie 。
Cookie 的作用是保存用户的浏览数据，对用户身份起到辨识作用，某些网站也会用 Cookie 来识别某个请求是否为爬虫。
+ Cookie 主要存放在浏览器的 Request Headers 中。

以京东搜索巧克力关键词的网页为例：
1. 打开开发者工具，选择 Network ；
2. 找到开头为 Search?keyword 的文件；
3. 点击 Headers，找到 Request Headers ；
4. 就能看到 Cookie 信息了。
+ 我们可以在请求头中设置 Cookie ， 在请求头中复制 cookie ，粘贴到代码中，以字典 key-value 的形式赋值给字典 headers 。
某些网站在反爬虫时，也会检查请求头中的 Cookie 或其他信息，但比较常见的是检查 User-Agent 

***
+ 怎么在其他网站查找JSON文件？
其实，网站存数据没有完全统一的规律，数据存到哪里都是网站开发者来决定的，这里介绍两个常用方法。
1. 打开Network检索要查找内容关键词，查看检查的文件数据；
2. 去网上以关键词检索：网站 数据，有的小伙伴会给出解决方案。

***
+ 用 readlines() 将文档中的数据以列表的形式输出
+ eval() 函数能够将字符串当成有效表达式来执行。

例如：
直接输出 dict_str 的类型时，由于内容被双引号包围所以输出为 <class 'str'> 表示是字符串格式
<img width="2178" height="919" alt="屏幕截图_10-8-2026_191328_np baicizhan com" src="https://github.com/user-attachments/assets/8915731b-92b6-4646-9fd7-2633d0f4e378" />
***
+ 节点选择器
1. 定位节点
节点选择器直接调用节点的名称就能定位到节点。
示例的 html 代码中要想定位到 <title></title> 节点，就可以使用 soup.title 来表示。
2. 获取名称
调用节点的名称时，使用 .name 属性就能定位到名称。
示例的 html 代码中要想提取 <title></title> 节点的名称，就可以使用 soup.title.name 来表示。
3. 获取属性
+ 调用节点时，使用 .attrs 就能定位到节点的属性。如果节点中有多个属性，就会全部输出。
示例的 html 代码中要想提取 <p></p> 节点中所有的属性，就可以使用 soup.p.attrs 来表示。
<p> 节点中有两个属性，以字典的形式输出
<img width="796" height="473" alt="屏幕截图_10-8-2026_213342_np baicizhan com" src="https://github.com/user-attachments/assets/d5377898-f220-432b-a369-be4d0ec1681c" />

+ 如果只提取某个属性的属性值，可以使用 .attrs[属性] 。
示例的 html 代码中要想提取 <p></p> 节点中 class 的属性值，就可以使用 soup.p.attrs["class"] 来表示。
想要提取 <p> 节点中 name 的属性值，使用 soup.p.attrs["name"] 来表示。
<img width="745" height="405" alt="屏幕截图_10-8-2026_213510_np baicizhan com" src="https://github.com/user-attachments/assets/4e0e07ae-12dd-4749-80a5-fd2d81960009" />

<img width="2222" height="1154" alt="屏幕截图_10-8-2026_213612_np baicizhan com" src="https://github.com/user-attachments/assets/67010b07-c8e1-4d9b-9223-69606695ffbe" />
<img width="1735" height="1011" alt="屏幕截图_10-8-2026_21374_np baicizhan com" src="https://github.com/user-attachments/assets/1409781a-2fdd-45a7-bf63-f28bcc1a0070" />
4. 获取内容
+ 当节点下同时包含了多个子节点时，需要使用 .text 属性提取节点中的全部内容。
例如，要提取 html 中 p 节点的全部内容，可以使用 soup.p.text 即可。

+ 当节点下同时包含了多个子节点时，也可以定位到下一级节点，提取该节点中的内容。
例如，要提取 html 中 p 节点中第一个 em 节点的内容，可以使用 soup.p.em.string 即可。
使用节点选择器时，在某个节点中同时存在两个相同的节点时，节点选择器只能调用第一个出现的节点，代码中第二个em标签中的内容就不会输出。
<img width="1357" height="361" alt="屏幕截图_10-8-2026_214032_np baicizhan com" src="https://github.com/user-attachments/assets/280f0e3e-a75d-4b5f-aaad-e482b8d89aba" />

***
# 将数据写入Excel文档
+ 需要学写入的数据样式、打开Excel方法、写入函数、保存数据并关闭
+ pandas 按照列的方式写入数据，每列的第一行是标题，其余是内容

<img width="2199" height="803" alt="屏幕截图_10-8-2026_22353_np baicizhan com" src="https://github.com/user-attachments/assets/2f0ea45e-713a-459e-bb75-e2c89a693d16" />

## 打开Excel方法
+ 我们要将数据写入Excel文档中，那么需要使用 pd 调用 ExcelWriter() 函数，该函数可用于打开指定的Excel文档。
括号中要传入文档的存储路径，若打开的文档不存在则新建文档再将数据写入。
## 写入函数
+ 将数据 info 写入Excel文档，需要使用 `to_excel()` 函数，使用 info 调用 `to_excel()` 函数就表示将数据该数据写入Excel文档。
+ `to_excel()` 函数需要传入两个参数 excel_writer, sheet_name
## 第一个参数 excel_writer 为文档，表示要将数据写入到哪个文档。
+ 第二个参数 sheet_name 为工作表名称，表示数据要写入该文档的哪个工作表。
+ 示例中，将数据写入 writer 文档，使用 `excel_writer=writer`；工作表名称为七年级学生信息， `sheet_name="七年级学生信息"`
## 保存和关闭
+ 完成了数据的写入后，还需要调用 `save(`) 函数保存数据；
+ 数据保存完成后，不需要对Excel文档进行操作**还需要使用 `close()` 函数关闭文档**。
**注意：由于模块更新，在本地运行遇到 save函数报错时，请使用 `writer._save()` 函数**
  
***

+ 想要获取子节点，我们可以**使用 `.contents` 属性，得到的结果是包含子节点的列表**

***
# 爬虫套路
1. 打开目标网页查看网页源代码，找到目标URL；
2. 找到URL后，就向网页发送请求；
3. 拿到请求后的数据后，按照节点提取信息；
4. 最后，将数据保存下来或做可视化操作。

## 分析网页
1. 一般的网页信息就在网页源代码中，我们只要找到网页源代码，根据节点就能提取到信息
2. 部分网站的数据是通过调用 API接口 （*API 的全称是应用编程接口。通俗易懂的说，API 其实就是别人已经写好的可以实现特定功能的函数*）来展示的，在爬取这类网站时，我们要分析信息存储地址，找到对应的 API接口 去请求数据
3. 还有一些网页会使用动态加载技术，将信息存储在JavaScript文件中。
在爬取这类网站中的内容时，我们要在开发者模式中，找到文件类型为script，数据格式为JSON格式，请求文件链接解析JSON数据。

## 请求网页
通过分析网页结构，我们就找到了目标URL，接下来，需要请求网页。
1. 请求网页需要导入 requests模块 ，并调用该模块中的 get() 函数，少数网站我们直接使用requests.get()请求URL获取内容
2. 但大部分网站都设有反爬虫机制，所以请求网页时需要设置请求头参数，常见的在headers中以字典的格式带上User-Agent即可。
3. 还有许多网页设有其他反爬虫措施，不仅检查User-Agent信息还会检查请求头中的Cookie信息，用于辨别用户的身份

## 提取数据
1. 拿到网页源代码后，常见的HTML格式的网页需要导入bs4模块来解析网页内容，课程中绝大部分项目都可以使用bs4模块来解析。
2. 但有时我们获取到的数据是乱码的，这是由于网页编码和字体反爬虫等原因造成的
3. 但当我们从JavaScript文件中提取信息时，由于该文件中的内容使用JSON格式（*用来存储和传输数据的格式，通常用于服务端向网页传递数据。
数据以 {键:值} 的形式呈现，数据与数据之间以逗号分隔，方括号[]表示数组，大括号{}用于保存对象*）存储数据，这时需要根据JSON格式来提取内容

### 我们在提取节点中的文本内容学了 .string 属性， .text 属性和 .contents 属性。
#### 那么三者到底有什么区别？
1. .string提取单个节点中的文本内容，若存在多个节点就会输出None；
2. .text即可提取单个节点，也可提取多个节点信息，以字符串格式输出；
3. .contents提取多个节点信息，并以列表形式输出。

## 保存分析
+ 提取到数据后，可以直接输出或将数据保存到文档中，课程中讲解了两种保存方式，使用with...as语句配合open()函数来保存文件，使用pandas模块将数据写入到Excel文档中
