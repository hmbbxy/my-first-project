# 基础知识
## 生物信息识别认证
+ 狭义的表述上，认为人脸识别是通过比较人脸视觉特征信息为手段的一种身份验证技术。
+ 它的应用场景与我们所熟知的指纹识别、虹膜识别等技术相同，是一种利用生物信息识别进行身份认证的技术。
## 人脸图像采集
+ 人脸图像采集包含了人脸检测、五官定位、人脸属性识别等操作。
1. 人脸检测
+ 识别图像中的人脸区域并给出人脸区域的矩形坐标，是人脸识别的基础功能。
2. 五官定位
+ 若要实现类似于“美颜”、“脸部卡通特效”等更精准的脸部修改，需要程序在检测到人脸的同时，精准定位出五官的位置。
3. 人脸属性识别
+ 若要对识别到的人脸进行更深层次的分析，比如判断“年龄”、“颜值”等信息，我们需要对脸部信息进行属性预测。
## 二进制数据的弊端
+ 直接将数据转化为多个由0或1组成的二进制数字，生成的结果太长不方便我们进行阅读、存储或传输的操作。    
## BASE64编码
+ Base64（基底64）是一种基于64个可打印字符来表示数据的方法，是一种编码方式。
+ Base64 编码将二进制数据转化为指定的64个可读字符，这样做能够在一定程度上缩小数据的长度，方便记录存储。
+ 数据不再是0、1组成的二进制，而是一些常用的字符。
+ 不仅如此，base64还可以将非文本内容如**图片、视频等数据**，转换为**文本形式**。这在只支持文本传输的协议下，非常实用。
## 编码表
+ 电脑为了简化计算会将一切数据根据规则转换为由0或1组成的二进制数字，这个规则由编码表来定义。
+ 编码表用来记录某种信息转换规则，比如计算机依靠ASCII编码规则可以将26个字母转化为数字。



***
# 在百度云平台创建人脸识别项目并获取接口信息

# 对图像进行人脸检测，实现这个过程需要两步：
1. 使用base64编码方法将图片转换成字符串；
2. 使用detect方法进行人脸检测：

+ 为什么要将图片转换为字符串
  + Aipface 中的detect接口能够对传入的图片进行检测，并返回图片中的人脸信息。
  + 但在这个过程中，传入的图片必须经过特殊处理转换为字符串类型。
  + 这就需要我们对图片进行编码操作。
    
## 将图片转化为字符串
+ 完成这个过程的代码需要3步：

1. 打开并读取指定的图片文件；
2. 利用base64模块进行编码；
3. 将编码结果转化为字符串；
<img width="870" height="582" alt="编码图片的步骤" src="https://github.com/user-attachments/assets/c35a8818-5966-49b6-9960-906aeb4dee9e" />

    

